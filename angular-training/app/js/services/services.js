'use strict';

var services = angular.module('services', []);

/** Application configuration constants. */
services.constant('configurationData', { mode: 'development', version: '1.0', angularVersion: '1.1.5' });